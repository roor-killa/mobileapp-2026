name: mobileapp_prjtst
description: BKN Wallet student project (Flutter + Supabase) - simple version.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: "^3.10.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  shared_preferences: ^2.5.4
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.2.3
  http: ^1.2.2
  flutter_secure_storage: ^9.2.2

  app_links: ^6.4.0
  url_launcher: ^6.3.0
  webview_flutter: ^4.7.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

  flutter_launcher_icons: ^0.14.2
  flutter_native_splash: ^2.4.4
flutter:
  uses-material-design: true

  assets:
    - assets/branding/
    - assets/legal/
