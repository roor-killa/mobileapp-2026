import 'package:flutter/material.dart';

class AppColors {
  // Fond global
  static const Color background = Color(0xFFE5E5E5); // Gris clair de l'image
  static const Color appBarBackground = Color(0xFFE60000); // Rouge vif
  static const Color cardBackground = Color(0xFFFFFFFF); // Blanc pur
  
  // Couleurs principales
  static const Color primary = Color(0xFFE60000); // Rouge vif principal
  static const Color primaryGlow = Color(0xFFFF4D4D); // Rouge clair
  static const Color secondary = Color(0xFF990000); // Rouge foncé
  static const Color secondaryGlow = Color(0xFF990000);
  
  // Orbes de fond (Désactivé)
  static const Color orbCyan = Colors.transparent; 
  static const Color orbPink = Colors.transparent;

  // Textes
  static const Color textPrimary = Color(0xFF000000); // Noir
  static const Color textSecondary = Color(0xFF888888); // Gris texte
  static const Color textOnPrimary = Color(0xFFFFFFFF); // Blanc sur rouge
  
  // États
  static const Color expenseColor = Color(0xFFE60000); // Rouge vif
  static const Color incomeColor = Color(0xFF00B300); // Vert
  static const Color transferColor = Color(0xFF2196F3); // Bleu natif
  
  // Bordures
  static const Color borderColor = Color(0xFFE0E0E0);
  static const Color inputFillColor = Color(0xFFFFFFFF);
  static const Color glassBorder = Colors.transparent; 
  static const Color glassBackground = Colors.transparent; 
}
